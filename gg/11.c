#include <stdio.h>

void main(){
    int k,N,b=0,a;
    float x,c;
    scanf("%d", &k);
    scanf("%d", &N);
    x=k*100;
    for(int i=0;i<=N;i++){
        scanf("%d", &a);
        b+=a;
        c=(b/x);
        if(){

        }
        printf("%.1f\n", c);
    }
}
